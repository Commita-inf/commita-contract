version: '3'
services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - 3000:3000
    volumes:
      - .:/app
    command: npm run dev
    environment:
      - NODE_ENV=development
